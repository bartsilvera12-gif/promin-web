# Local Claude Code settings (personal, machine-specific)
.claude/settings.local.json

# Dependencies (installed outside the repo, but just in case)
node_modules/

# OS / editor cruft
Thumbs.db
.DS_Store
*.log

# Imágenes subidas desde el panel admin (las escribe upload.php en el server;
# no deben ser versionadas ni pisadas por el deploy de GitHub)
/uploads/site/
/uploads/gallery/

# Galería: la fuente VIVA la escribe el servidor (api/gallery.php); no versionar.
# La semilla data/gallery.seed.json SÍ se versiona (es el respaldo/base).
/data/gallery.json
